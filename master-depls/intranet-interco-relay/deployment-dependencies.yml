---
deployment:
  bosh-deployment:
    resources:
      secrets:
        local_deployment_scan: false
      templates:
        extended_scan_path:
          - shared-operators/docker-images
          - shared-operators/paas-templates-version
    releases:
      networking:
      haproxy:
      os-conf:
      prometheus:
      docker:
      squid:
