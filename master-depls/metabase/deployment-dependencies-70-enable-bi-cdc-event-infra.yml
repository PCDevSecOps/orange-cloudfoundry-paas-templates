---
deployment:
  bosh-deployment:
    resources:
      templates:
        extended_scan_path:
          - shared-operators/docker-images
    releases:
      kafka:
      zookeeper:
      generic-scripting:
      docker:
      bosh-dns-aliases:
