---
deployment:
  internet-proxy:
    resources:
      secrets:
        local_deployment_scan: true
      templates:
        extended_scan_path:
          - shared-operators/docker-images
    releases:
      docker:
      generic-scripting: