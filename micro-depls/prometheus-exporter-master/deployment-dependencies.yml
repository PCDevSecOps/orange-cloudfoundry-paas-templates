---
deployment:
  prometheus-exporter-master:
    resources:
      templates:
        extended_scan_path:
        - "submodules/prometheus-boshrelease"
        - shared-operators/paas-templates-version
    releases:
      routing:
      prometheus:
      bosh-dns-aliases:
