---
deployment:
  k8s-minio:
    resources:
      templates:
        extended_scan_path:
        - shared-operators/k8s-common
    stemcells:
    releases:
      helm-kubectl:
      generic-scripting:
      minio:

    errands:
      action:

    manual-errands:
      errand-scripting:
        display-name: mirror-from-legacy-minio

