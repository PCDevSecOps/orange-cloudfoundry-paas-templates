---
deployment:
  bosh-deployment:
    resources:
      templates:
        extended_scan_path:
        - shared-operators/multi-region-common/r1
        - shared-operators/multi-region-common/r3

    releases:
      jumpbox:
      bosh-dns-aliases:
      os-conf:
      wireguard:

    errands:

