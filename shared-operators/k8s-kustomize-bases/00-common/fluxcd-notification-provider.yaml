apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Provider
metadata:
  name: slack
spec:
  type: slack
  channel: ((/secrets/multi_region_region_1_slack_notifications_channel))
  secretRef:
    name: slack-url-secret