---
deployment:
  bosh-deployment:
    resources:
      templates:
        extended_scan_path:
        - shared-operators/k8s-common
        - shared-operators/paas-templates-version
    stemcells:
    releases:
      helm-kubectl:

    errands:
      action:

    manual-errands:


