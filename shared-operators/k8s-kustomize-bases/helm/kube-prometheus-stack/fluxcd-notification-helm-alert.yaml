apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Alert
metadata:
  name: alert-on-helm-release-sync-failure
spec:
  summary: HelmRelease sync failure on paas-templates-k8s-cluster
  providerRef:
    name: slack
  eventSeverity: error
  eventSources:
    - kind: HelmRelease
      name: '*'
