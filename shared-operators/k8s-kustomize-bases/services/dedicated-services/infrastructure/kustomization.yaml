apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
#no namespace specified (each child kustomization has its own ns)
resources:
- ../../../helm/kyverno
- ../../../helm/fluxcd
patchesStrategicMerge: []
